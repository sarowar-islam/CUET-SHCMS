import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.cuet.shcms',
  appName: 'CUET SHCMS',
  webDir: 'dist',
  android: {
    backgroundColor: '#F4F5F7',
  },
}

export default config
